import { action, reaction, makeObservable } from 'mobx';

export default class LoginnStore {
    constructor(token:string, is_logged_in:boolean) {
        makeObservable(this, {
           
        });



    }
}